# Animarr Configuration

# Port for the Animarr server (default 5000)
PORT=5000

# Optional: Password to protect the Animarr dashboard. 
# If empty, no authentication is required (useful if behind SSO like YunoHost).
ADMIN_PASSWORD=

# Prowlarr Configuration
PROWLARR_URL=http://localhost:9696
PROWLARR_API_KEY=your_prowlarr_api_key_here

# External API Keys (TMDB, IMDB/OMDB) - Anilist and MAL (Jikan) usually don't require keys for basic search
TMDB_API_KEY=
IMDB_API_KEY=
